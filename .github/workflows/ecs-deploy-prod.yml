name: Build, Push, and Deploy PROD

on:
  workflow_dispatch: # 手動実行

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  ECR_REGISTRY: ${{ secrets.AWS_ECR_REGISTRY }}

jobs:
  # ─────────────────────────────────────────────────
  # 1) AWS 認証 & ECR にログイン (本番環境)
  # ─────────────────────────────────────────────────
  ecr-login:
    environment: Production
    runs-on: ubuntu-latest
    outputs:
      login: ${{ steps.login-ecr.outcome }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

  # ─────────────────────────────────────────────────
  # 2) Backend イメージのビルド & Push (本番環境)
  # ─────────────────────────────────────────────────
  build-push-backend:
    environment: Production
    needs: ecr-login
    runs-on: ubuntu-latest
    outputs:
      image_uri: ${{ steps.build-image.outputs.image_uri }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build backend image with commit SHA tag
        id: build-image
        run: |
          IMAGE_TAG=$(echo $GITHUB_SHA | head -c7)
          IMAGE_URI="${{ env.ECR_REGISTRY }}/prod-backend:$IMAGE_TAG"
          LATEST_URI="${{ env.ECR_REGISTRY }}/prod-backend:latest"
          echo "Building and tagging images: $IMAGE_URI and $LATEST_URI"
          docker build \
            --file backend/Dockerfile \
            --tag  $IMAGE_URI \
            --tag  $LATEST_URI \
            backend
          echo "::set-output name=image_uri::$IMAGE_URI"

      - name: Push backend to ECR
        run: |
          docker push ${{ steps.build-image.outputs.image_uri }}
          docker push ${{ env.ECR_REGISTRY }}/prod-backend:latest

  # ─────────────────────────────────────────────────
  # 3) Frontend イメージのビルド & Push (本番環境)
  # ─────────────────────────────────────────────────
  build-push-frontend:
    environment: Production
    needs: ecr-login
    runs-on: ubuntu-latest
    outputs:
      image_uri: ${{ steps.build-image.outputs.image_uri }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build frontend image with commit SHA tag
        id: build-image
        run: |
          IMAGE_TAG=$(echo $GITHUB_SHA | head -c7)
          IMAGE_URI="${{ env.ECR_REGISTRY }}/prod-frontend:$IMAGE_TAG"
          LATEST_URI="${{ env.ECR_REGISTRY }}/prod-frontend:latest"
          echo "Building and tagging images: $IMAGE_URI and $LATEST_URI"
          docker build \
            --file study-support-app/Dockerfile \
            --tag  $IMAGE_URI \
            --tag  $LATEST_URI \
            study-support-app
          echo "::set-output name=image_uri::$IMAGE_URI"

      - name: Push frontend to ECR
        run: |
          docker push ${{ steps.build-image.outputs.image_uri }}
          docker push ${{ env.ECR_REGISTRY }}/prod-frontend:latest

  # ─────────────────────────────────────────────────
  # 4) Backend を ECS にデプロイ (本番環境)
  # ─────────────────────────────────────────────────
  deploy-backend-prod:
    environment: Production
    needs: build-push-backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}
      - name: Deploy Backend to ECS
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          cluster: prod-api
          service: prod-api-service
          task-definition: AWS/backend-task-def-prod.json
          container-name: backend
          image: ${{ env.ECR_REGISTRY }}/prod-backend:latest
          wait-for-service-stability: true

  # ─────────────────────────────────────────────────
  # 5) Frontend を ECS にデプロイ (本番環境)
  # ─────────────────────────────────────────────────
  deploy-frontend-prod:
    environment: Production
    needs: build-push-frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}
      - name: Deploy Frontend to ECS
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          cluster: prod-front
          service: prod-front-service
          task-definition: AWS/frontend-task-def-prod.json
          container-name: frontend
          image: ${{ env.ECR_REGISTRY }}/prod-frontend:latest
          wait-for-service-stability: true 